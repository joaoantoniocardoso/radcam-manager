// TODO: Generate this file via RUST
export type Camera = {
    uuid: string
    hostname: string
    credentials: {
        username: string
        password: string
    }
    streams: Record<string, unknown> // Adjust as needed
}
